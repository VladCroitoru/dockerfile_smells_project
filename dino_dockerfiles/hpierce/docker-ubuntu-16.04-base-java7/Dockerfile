#
# Dockerfile
#
#
FROM hpierce/docker-ubuntu-16.04-base

# Environment
ENV DEBIAN_FRONTEND noninteractive

# java 7 install
RUN add-apt-repository ppa:openjdk-r/ppa && apt-get update && apt-get install openjdk-7-jdk -y

