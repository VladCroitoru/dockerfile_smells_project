from thomaschln/r-devtools

run apt-get update && apt-get install -t unstable -y libxml2-dev npm git

run npm install git+https://git@github.com/arthurlacoste/google-translate-api.git

run R -e "install.packages(c('lubridate', 'twitteR', 'RSQLite', 'ROAuth', 'plyr', 'ggmap', 'XML'))"
