FROM debian:latest

LABEL description="Simple docker image with supervisor installed. Image is based on debian jessie."
LABEL maintainer="todstoychev@gmail.com"
LABEL version="0.1.0"

RUN apt-get update && apt-get install supervisor -y
